#ifndef GUI_MAIN
#define GUI_MAIN

#include <GL/glfw.h>

/**
  * @brief create a program window with default arguments.
  * @return zero for successful operation; non-zero otherwise.
  */
int initWindow();

#endif //GUI_MAIN
